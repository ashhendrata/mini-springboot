CREATE TABLE user_fields {
    internalid INT(6) AUTO_INCREMENT PRIMARY KEY,
    firstname VARCHAR(30) NOT NULL,
    lastname VARCHAR(30) NOT NULL,
    id INTEGER,
    creationtime VARCHAR(30) NOT NULL

    };